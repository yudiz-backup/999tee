query verifyForgetPassword(
    $mobileNumber: String
    $otp: String
    $websiteId: Int
    ){
  forgotPassworOTPVerify(
      mobileNumber: $mobileNumber, 
      otp: $otp,
      websiteId: $websiteId) {
    message
    status
  }
}