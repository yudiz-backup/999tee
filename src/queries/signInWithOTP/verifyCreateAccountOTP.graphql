query verifyCreateAccountOTP(
    $mobileNumber: String
    $otp: String
    $websiteId: Int
){
  createAccountOTPVerify(
      mobileNumber: $mobileNumber, 
      otp: $otp, 
      websiteId: $websiteId
  ) {
    message
    status
  }
}